const { prefix } = require('../require.js');
const { sendEmbed, sendError } = require('../lib/functions.js');

module.exports = {
    name: 'name',
    aliases: [ 'alias' ],
    category: 'category',
    // admin, mod, fun, else
    description: "desc",
    usage: `${prefix}usage`,
    exemple: `${prefix}exemple`,
    access: 'permission',
    // "BOTOWNER" can be used
    execute(message, args){
        // code
    }
}